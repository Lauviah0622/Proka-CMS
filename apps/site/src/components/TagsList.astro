---
const { tags } = Astro.props
---

<style>
  .tags {
    display: flex;
    color: var(--color-text-secondary);
    gap: 0.8ch;
  }
</style>
<div class={'tags'}>
  {
    tags.map((tag) => (
      <a class="tag" href={`/tags/${tag}`}>
        {tag}
      </a>
    ))
  }
</div>
